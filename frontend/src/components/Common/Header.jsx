import React from "react";
import Topbar from "../Layout/Topbar";

const Header = () => {
  return (
    <header>
      {/* Topbar */}
      <Topbar />
      {/* navbar */}
      {/* cart drawer */}
    </header>
  );
};

export default Header;
